sources:
  docker_logs:
    type: docker_logs

sinks:
  file:
    type: file
    inputs:
      - docker_logs
    path: /var/log/vector/{{ container_name }}/current.log
    encoding:
      codec: json
