timezone: Asia/Seoul

sources:
  docker_logs:
    type: docker_logs

sinks:
  file:
    type: file
    inputs:
      - docker_logs
    encoding:
      codec: text
    compression: zstd
    path: /logs/{{ container_name }}/%Y-%m-%d.log.zst
